# brocade 控制面的环境变量。
#
# 部署时抄一份到机器上（约定是 /opt/brocade/console.env），由 systemd 的
# EnvironmentFile= 喂给进程。程序自己不读 .env 文件，所以这份模板只有被抄过去才生效。
#
# 抄的时候整份抄，别只挑看得懂的几行：下面带默认值的那些不是可选装饰，
# BROCADE_XRAY_VERSION 留空的后果在它自己那一段里写了。
#
# Agent 请求地址和 xray 版本现在也能在控制台的「设置 → 分发」里改，改了立刻生效、不必重启。
# 库里填过就以库里的为准，这份文件退化成初值——所以这两行的意义是「新部署开箱就落在
# 能跑的区间里」，而不是「唯一的真相源」。

# ── 必填 ──

# PostgreSQL。库不存在就自己建，表结构也由进程启动时自己迁移——所以新部署只要有个
# 能连上的服务器和一个有 CREATEDB 权限的角色，不用先手工 createdb。
#
# 代价你要知道：「库不存在」和「库名打错了」在服务器看来是同一件事。打错的话它会
# 把那个错名字建出来、迁移好、然后端上一个空控制台，看着完全正常。所以真建了的那次
# 启动会先打一行 `created the database "…"`，写着库名——起服务时扫一眼那行。
DATABASE_URL=postgres://brocade:CHANGE_ME@127.0.0.1:5432/brocade

# 密封库里那两样不能明文存的东西：DNS 凭据和每台节点的证书私钥。
#
# 不配也能跑——不用证书功能的部署完全用不到它。但一旦要用，**没有这个就存不了**：
# 保存凭据会当场报错，而不是退回明文存一份。退回明文会得到一个「看着加密了其实没有、
# 而且没人分得清哪行是哪样」的库。
#
# 生成：openssl rand -base64 32
#
# 丢了就找不回来。库里已经密封的东西解不开，只能把 DNS 凭据重填一遍、让每台节点重签一张
# 新证书——不是灾难，但要动手。跟 DATABASE_URL 一起备份。
# BROCADE_SECRET_KEY=

# ── 监听 ──
#
# 两个面：控制台 API 和节点 API。默认都是 0.0.0.0，生产上通常绑回环、前面挡一层反代。
BROCADE_ADMIN_BIND=127.0.0.1:8080
BROCADE_AGENT_BIND=127.0.0.1:8081

# 控制台前端。默认不用配：它和 agent 一样编在控制面二进制里，部署就是拷一个文件。
#
# 设成一个目录，就改从那个目录读，二进制里带的那份不再参与。用途是不重编控制面就换前端——
# 本地改前端时 npm run build 完刷新一下，或者机器上要临时打个补丁。代价是从此没人保证
# 页面和它旁边那个 API 是同一个版本，这正是内嵌要解决的问题，所以别拿它当常规部署方式。
# 相对路径按进程的工作目录算，systemd 下写绝对路径。
#
# BROCADE_CONSOLE_DIST=/opt/brocade/frontend/dist

# ── 分发 ──

# 节点从哪儿下载 agent 和 install.sh，也是贴到新机器上那条命令里的地址。
#
# 必须是**从节点那一侧**够得着的地址，不是上面那个 BROCADE_ADMIN_BIND。控制面常绑回环、
# 前面挡一层反代，那个地址在别的机器上什么都不是。不设的话退回 http://127.0.0.1:8080，
# 于是装机命令会让新机器去连它自己，第一次 fetch 就失败。
BROCADE_AGENT_PUBLIC_URL=https://console.example.net

# 全队装哪个 xray，写上游的 release tag。
#
# **别留空。** 留空时装机脚本取 latest，而 latest 在这套控制面里是坏的：它依赖的两个上游
# 特性没有超过一个版本的重叠区间——geodata 运行时热加载要 >= 26.4（app/geodata 落在
# 2026-04-25），而 VLESS 反向隧道从 26.5 起载得出去、载不回来（上游 #6242，在
# 26.5.3 / 26.5.9 / 26.6.27 / 26.7.28 上用同一份配置复现，26.4.25 正常）。
#
# 症状不是装不上，是装得干干净净、报进来一切正常，然后反向那条链一个字节都回不来。
# 这个数字也正是仓库里 .tools/xray 那一版——测试拿它跑 `xray -test` 校验编译出来的配置，
# 换版本时两处一起换。
BROCADE_XRAY_VERSION=v26.4.25

# ── 分发（可选）──
#
# 下面这些不设也能跑，各有各的退路：agent 用编译进控制面的那一份，xray 和 phantun
# 从上游 release 下载。够不到 GitHub 的机队，或者要走自己 CDN 的，才需要它们。
#
# 每个 URL 都要配一个 sha256：装机脚本拿它验下载的字节，缺了就没得验。
# 填错的后果是新机器装上一个坏二进制，而 install.sh 会当场拒绝并说明是哪一个——
# 这是有意为之，装到一半失败好过装完了跑一个不对的东西。

# BROCADE_AGENT_BIN_URL=
# BROCADE_AGENT_BIN_SHA256=
# BROCADE_XRAY_BIN_URL=
# BROCADE_XRAY_BIN_SHA256=
# BROCADE_PHANTUN_SERVER_URL=
# BROCADE_PHANTUN_SERVER_SHA256=
# BROCADE_PHANTUN_CLIENT_URL=
# BROCADE_PHANTUN_CLIENT_SHA256=
